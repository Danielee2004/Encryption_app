{
  "name": "encryption-app",
  "version": "1.0.0",
  "description": "Clarinet + Vitest project for encrypted file transfer metadata on Stacks",
  "scripts": {
    "test": "vitest --runInBand"
  },
  "devDependencies": {
    "vitest": "^2.0.0",
    "@stacks/clarinet-sdk": "^0.5.0"
  }
}